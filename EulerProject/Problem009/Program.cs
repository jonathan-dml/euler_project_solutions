namespace EulerProject.Problem009;

public static class Program
{
    /// <summary>
    /// Returns the product abc of the Pythagorean triplet a &lt; b &lt; c (natural numbers,
    /// a² + b² = c²) for which a + b + c = <paramref name="sum"/>.
    /// </summary>
    public static long SpecialPythagoreanTripletProduct(int sum)
    {
        throw new NotImplementedException();
    }
}
