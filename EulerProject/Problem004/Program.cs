namespace EulerProject.Problem004;

public static class Program
{
    /// <summary>
    /// Returns the largest palindrome made from the product of two numbers
    /// that each have exactly <paramref name="digits"/> digits.
    /// </summary>
    public static long LargestPalindromeProduct(int digits)
    {
        throw new NotImplementedException();
    }
}
